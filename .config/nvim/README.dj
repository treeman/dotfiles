# External dependencies

- ripgrep
- fd
- git
- fzf
- libcurl-devel
- hurl
- jq
- luarocks

## Formatters

- prettierd (could move to mason...?

  ```bash
  npm install --save-dev --save-exact prettier
  npm install -g @fsouza/prettierd
  ```
- astyle (Java)

- pgformat (SQL)

- stylua

## Linters

- commitlint

  ```bash
  npm install -g --save-dev @commitlint/{cli,config-conventional}
  ```
